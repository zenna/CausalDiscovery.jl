"Parametric representation of expressions"
module ParamExpression

"""
A parametric representation of an expression

This encodes a function from some known primitives types to an expression.

```
θ::Bool -> Θ ? (+ 1 1) : (- 1 1)
```
```
"""
struct ParamExpr
end

end